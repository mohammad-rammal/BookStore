<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Check Your Email</title>
    <style>
        body {
            background: linear-gradient(135deg, #2193b0 0%, #6dd5ed 100%);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: Arial, sans-serif;
        }

        .email-card {
            background: #ffffff;
            padding: 2rem;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            width: 100%;
        }

        .email-card h3 {
            color: #007bff;
            margin-bottom: 1rem;
        }

        .email-card p {
            color: #495057;
            margin-bottom: 1.5rem;
        }

        .email-card h2 {
            color: #007bff;
            font-weight: bold;
            margin-top: 1rem;
        }

        .email-icon {
            font-size: 4rem;
            color: #007bff;
            margin-bottom: 1rem;
        }

        .countdown {
            font-size: 1.5rem;
            color: #dc3545;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div class="email-card">
        <div class="email-icon">
            <i class="bi bi-envelope-check-fill"></i>
        </div>
        <h2>Please Check Your Email</h2>
        <p>A link to reset your password has been sent to your email address.</p>

        <div class="countdown">Link Expires in: <span id="countdown-timer">20:00</span></div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ZcJ3s5zPA+F+mQABdbv7A9kHro5sS1bHdTkHl0UJep81g+xwlZ62eHTBhvj6KzRE" crossorigin="anonymous">
        </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>

    <script>
        const countdownElement = document.getElementById('countdown-timer');
        let timeRemaining = 20 * 60;

        const countdownInterval = setInterval(() => {
            const minutes = Math.floor(timeRemaining / 60);
            const seconds = timeRemaining % 60;

            countdownElement.textContent = `${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;

            if (timeRemaining <= 0) {
                clearInterval(countdownInterval);
                countdownElement.textContent = 'Expired';
            }
            timeRemaining--;
        }, 1000);
    </script>
</body>

</html>